import dependencies from './lib/dependencies'
import compile from './lib/compile'

export interface Options {
  save?: boolean
  saveDev?: boolean
  saveAmbient?: boolean
  cwd: string
}

export default function (src: string, options: Options) {

}
